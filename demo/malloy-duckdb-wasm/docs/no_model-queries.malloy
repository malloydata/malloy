import "no_model.malloy"

// --
// Name: Show Words Table
query: words -> {
  project: *
}
// --
// Name: Show Airports Table
query: airports -> {
  project: *
}

// --
// Name: Show Auto Recall Table
// I think this is broken because we don't handle dates correcty yet in WASM.
query: recalls -> {
  project: *
}