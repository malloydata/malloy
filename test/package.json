{
  "name": "test",
  "private": true,
  "scripts": {
    "lint": "eslint '**/*.ts{,x}'",
    "lint-fix": "eslint '**/*.ts{,x}' --fix",
    "test": "jest --config=../jest.config.js",
    "build": "tsc --build",
    "malloyc": "ts-node ../scripts/malloy-to-json"
  },
  "dependencies": {
    "@malloydata/db-bigquery": "^0.0.8",
    "@malloydata/db-duckdb": "^0.0.8",
    "@malloydata/db-postgres": "^0.0.8",
    "@malloydata/malloy": "^0.0.8"
  },
  "version": "0.0.8"
}
